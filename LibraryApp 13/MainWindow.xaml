<Window x:Class="LibraryApp13.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Библиотека" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Управление библиотекой" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Панель фильтрации и поиска -->
        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="0,0,0,10" CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Vertical" Margin="5">
                    <TextBlock Text="Поиск по названию:" Margin="0,0,0,5"/>
                    <TextBox x:Name="SearchTextBox" Height="25"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Vertical" Margin="5">
                    <TextBlock Text="Фильтр по автору:" Margin="0,0,0,5"/>
                    <ComboBox x:Name="AuthorFilterComboBox" Height="25" DisplayMemberPath="LastName" SelectedValuePath="Id"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Vertical" Margin="5">
                    <TextBlock Text="Фильтр по жанру:" Margin="0,0,0,5"/>
                    <ComboBox x:Name="GenreFilterComboBox" Height="25" DisplayMemberPath="Name" SelectedValuePath="Id"/>
                </StackPanel>

                <Button Grid.Column="3" Content="Применить" Width="100" Height="40" Margin="5" Click="ApplyFilter_Click" VerticalAlignment="Bottom"/>
            </Grid>
        </Border>

        <!-- DataGrid для книг -->
        <DataGrid Grid.Row="2" x:Name="BooksGrid" AutoGenerateColumns="False" IsReadOnly="True" 
                  SelectionMode="Single" Margin="0,0,0,10" RowHeight="30">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Title}" Width="200"/>
                <DataGridTextColumn Header="Автор" Binding="{Binding Author.LastName}" Width="120"/>
                <DataGridTextColumn Header="Жанр" Binding="{Binding Genre.Name}" Width="120"/>
                <DataGridTextColumn Header="Год" Binding="{Binding PublishYear}" Width="80"/>
                <DataGridTextColumn Header="ISBN" Binding="{Binding ISBN}" Width="150"/>
                <DataGridTextColumn Header="В наличии" Binding="{Binding QuantityInStock}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Панель кнопок -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Добавить книгу" Width="120" Height="35" Margin="5" Click="AddBook_Click"/>
            <Button Content="Редактировать" Width="120" Height="35" Margin="5" Click="EditBook_Click"/>
            <Button Content="Удалить книгу" Width="120" Height="35" Margin="5" Click="DeleteBook_Click"/>
            <Button Content="Авторы" Width="100" Height="35" Margin="5" Click="ManageAuthors_Click"/>
            <Button Content="Жанры" Width="100" Height="35" Margin="5" Click="ManageGenres_Click"/>
        </StackPanel>
    </Grid>
</Window>